import React from "react";

// user is redirected HERE after logging in with Auth0, then redirected to authcheck then home page

const Callback = (props) => <div>Callback</div>;

export default Callback;
